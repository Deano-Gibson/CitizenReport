name: Build MVC

on:
  push:
    branches: [ "MVC" ]
  pull_request:
    branches: [ "MVC" ]

jobs:
  build-mvc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore (MVC)
        run: dotnet restore ./CitizenReportWeb.csproj

      - name: Build (MVC)
        run: dotnet build ./CitizenReportWeb.csproj -c Release --no-restore

      - name: Test (if any)
        shell: bash
        run: |
          if ls **/*Tests.csproj **/*Test.csproj 1>/dev/null 2>&1; then
            dotnet test -c Release --no-build
          else
            echo "No test projects found. Skipping."
          fi

      - name: Publish artifact
        run: dotnet publish ./CitizenReportWeb.csproj -c Release -o ./artifacts --no-build

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: CitizenReportWeb
          path: artifacts
